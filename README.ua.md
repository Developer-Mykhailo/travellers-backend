[🇺🇸 English](README.md) | [🇺🇦 Українською](README.ua.md)

# Travellers App API

Travellers App — це backend для додатку, призначеного для мандрівників, які хочуть ділитися своїми історіями. Проєкт створений з використанням Node.js, Express.js, MongoDB та JWT.

## 🚀 Можливості

- **Авторизація користувачів:** реєстрація, логін, логаут.
- **OAuth через Google:** швидкий вхід через Google.
- **Відновлення паролю:** через email з JWT-токеном.
- **Керування профілем:** отримання, оновлення даних, завантаження/видалення аватарки.
- **Робота з історіями:** створення, перегляд, оновлення, видалення.
- **Фільтрація, сортування, пагінація:** для історій та користувачів.
- **Збережені історії:** додавання/видалення, підрахунок вподобайок.
- **Категорії:** отримання списку, прив’язка до історій.
- **Завантаження файлів:** фото до історій та аватарок (Cloudinary або локально).
- **Захист маршрутів:** доступ за токеном.
- **Валідація даних:** захист від некоректного вводу (Joi).
- **Безпека:** захист від XSS, SQL-ін'єкцій, транзакції MongoDB.
- **Логування:** через pino-http.

## 🛠 Технології

- **Node.js** — серверна платформа
- **Express.js (v5.1.0)** — фреймворк для створення API
- **MongoDB / Mongoose (v9.0.0)** — база даних
- **JWT** — автентифікація
- **Bcrypt** — хешування паролів
- **CORS** — керування доступом між доменами
- **Joi (v18.0.2)** — валідація даних
- **Cloudinary** — зберігання файлів
- **Multer** — завантаження файлів
- **Nodemailer** — відправлення листів
- **Helmet** — захист заголовків
- **Swagger UI** — документація API
- **Dotenv** — змінні середовища
- **Pino** — логування
- **Redocly** — генерація документації
- **ESLint** — перевірка якості коду
- **Nodemon** — автоматичний перезапуск сервера в dev-режимі

## 📁 Структура проєкту

```
├── docs/                 # Документація OpenAPI (Swagger)
├── node_modules/         # Залежності npm
├── src/                  # Головна папка з кодом
│   ├── constants/        # Константи (наприклад, JWT-терміни, SMTP)
│   ├── controllers/      # Контролери — логіка обробки запитів
│   ├── db/               # Підключення до бази даних
│   │   ├──models/        # Моделі Mongoose (User, Story, Category)
│   ├── middlewares/      # Мідлвари — валідація, автентифікація, логування
│   ├── routers/          # Маршрути API (auth, stories, users)
│   ├── services/         # Сервіси — бізнес-логіка (auth.js, stories.js, users.js)
│   ├── templates/        # Шаблони для email (наприклад, reset-password-email.html)
│   ├── utils/            # Допоміжні функції (getEnvVar, sendMail, calculatePaginationData тощо)
│   ├── validation/       # Валідація Joi (schema для запитів)
│   ├── index.js          # Ініціалізація сервера
│   └── server.js         # Головний файл сервера
├── swagger/              # Файли OpenAPI (components, paths, schemas)
│   ├── components/
│   ├── responses/
│   ├── schemas/
│   └── paths/
│       ├── auth/
            └── google/
│       ├── stories/
│       └── users/
├── temp/                 # Тимчасові файли (може не використовуватися)
├── uploads/              # Локальне зберігання завантажених файлів (якщо не використовується Cloudinary)

```

## 📚 API Документація

Повну документацію з прикладами запитів та відповідей можна переглянути тут: [Swagger UI →](https://travellers-backend.onrender.com/api-docs/)

## 🔍 Деталі функціоналу

- **Авторизація:** реєстрація, логін, логаут, OAuth через Google, відновлення паролю.
- **Сесії:** JWT-токени, автоматичний refresh, видалення при логауті.
- **Користувачі:** отримання списку (фільтрація за ім’ям/іменами), пошук, сортування, пагінація, отримання профілю, завантаження/видалення аватарки, збережені історії.
- **Історії:** створення, оновлення, видалення, фільтрація (за власником/власниками, категорією/категоріями), пошук, сортування, пагінація.
- **Файли:** завантаження фото до історій та аватарок, збереження в Cloudinary або локально.
- **Безпека:** валідація Joi, транзакції MongoDB, захист від XSS, захист маршрутів.
- **Категорії:** отримання списку категорій, прив’язка до історій.
- **Збережені історії:** додавання/видалення, підрахунок улюблених.

## 🧪 Як запустити локально

**1. Клонуй репозиторій:**

```
bash

git clone https://github.com/Developer-Mykhailo/travellers-backend.git
```

**2. Встанови залежності:**

```
bash

npm install
```

**3. Створи файл .env на основі .env.example:**

```
env

PORT=3000
MONGODB_URI=...
JWT_SECRET=...
CLOUDINARY_URL=..
```

**4. Запусти сервер:**

- У продакшн:

```
bash

npm start
```

- У розробці:

```
bash

npm run dev
```

## 🧹 Додаткові команди

- **npm run lint** — перевірити код на відповідність стандартам (ESLint)
- **npm run build-docs** — згенерувати JSON з OpenAPI
- **npm run preview** — локально переглянути документацію (Redocly)

## ✅ План розвитку

- Коментарі до історій
- Вподобайки/дизлайки

## 📬 Контакти

Якщо є питання — звертайся: developer.mykhailo@gmail.com
